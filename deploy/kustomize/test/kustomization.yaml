apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: sealos-nm-system
namePrefix: sealos-nm-agent-


resources:
- configmap.yaml
- secret.yaml
- pod.yaml

images:
- name: agent-test
  newName: dinoallo/sealos-nm-agent-test
  newTag: latest
labels:
- includeSelectors: true
  pairs:
    app.kubernetes.io/name: agent-test
